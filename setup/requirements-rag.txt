# RAG Dependencies — vector database and retrieval
-r requirements-core.txt
chromadb>=0.4.22
# Only RecursiveCharacterTextSplitter is used (api/services/chunker.py).
# Capped below 0.2 because the text_splitter module moved to a separate
# langchain-text-splitters package in 0.2; revisit when we adopt the new layout.
langchain>=0.1.4,<0.2
sentence-transformers>=2.2.2
pillow>=12.2.0
