Adoption-ladder dots on a thread header: seed → shape → chain → formalize → operate. `hot` glows the next rung when a graduation is available (e.g. enough pinned steps to convert).

```jsx
<MaturityMeter value={2} />
<MaturityMeter value={3} hot label="maturity" />
```
