[pytest]
markers =
    integration: marks tests that require Ollama running with models (deselect with '-m "not integration"')
    e2e: requires live Ollama service (skipped in CI)
    rag: requires RAG dependencies (langchain, chromadb, sentence-transformers — skipped in core CI)
testpaths = tests
