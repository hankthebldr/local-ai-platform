# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST
*.manifest
*.spec
pip-log.txt
pip-delete-this-directory.txt
.tox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
.hypothesis/
.pytest_cache/

# Virtual environments
venv/
env/
ENV/
env.bak/
venv.bak/
.venv/

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Jupyter Notebook
.ipynb_checkpoints

# Environment
.env
.env.local
*.env

# Stray logs at any depth (data/logs/* already covered)
*.log

# Models and data (large files)
data/models/*.gguf
data/models/*.bin
data/models/*.safetensors
data/models/*.pth
data/models/*.pt
data/models/*.ckpt
data/vectors/chroma_db/
data/cache/**
data/logs/*.log

# Fine-tuning outputs
finetuning/outputs/**/*.bin
finetuning/outputs/**/*.safetensors
finetuning/outputs/**/*.pth
finetuning/datasets/**/*.jsonl

# Temporary files
*.tmp
*.temp
*.bak
*.backup
.cache/

# Docker
docker-compose.override.yml

# OS
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# Archives
*.zip
*.tar.gz
*.rar

# Keep directory structure
!data/.gitkeep
!data/models/.gitkeep
!data/vectors/.gitkeep
!data/cache/.gitkeep
!data/logs/.gitkeep
!data/workflows/.gitkeep

# Workflow run data (persisted locally)
data/workflows/*/

# Brainstorm session files
.superpowers/

# ── Runtime user data — never commit ────────────────────────────────
# API key hashes and metadata (managed via /api/keys endpoints)
data/config/api_keys.yaml
# User-pinned memories and session summaries
data/memory/sessions/
data/memory/facts.yaml
data/memory/index.yaml
# Per-conversation sandboxes (wiped when session closes)
data/sandboxes/
# RAG document storage and ChromaDB
data/rag/
# Exported session markdown
data/exports/
# Graph cache
data/graph/
# Playwright artifacts
.playwright-mcp/

# ── Secrets (narrow — DO NOT add blanket *.pem/*.key globs) ─────────
# Narrow suffix-scoped patterns prevent real private material from
# landing in git while still allowing any bona-fide public keys or
# certificates to be committed by their natural filenames.
**/*_privkey.pem
**/*-private.pem
**/*.private.key
**/id_rsa
**/id_ed25519
*secret*.yaml
*secret*.yml
*secret*.json
*credentials*.yaml
*credentials*.yml
*credentials*.json

# ── Build artifacts ─────────────────────────────────────────────────
# macOS DMG output (dist/ already ignored; this catches stray copies)
*.dmg
*.icns.backup
data/logs/*.jsonl

# ── Claude Code worktrees (project-local isolation) ─────────────────
.claude/worktrees/
.worktrees/
.superpowers/
